import { ManagerFeaturePage } from "@/features/startup-workspace/ManagerScreens";

export const metadata = { title: "기관 리포트" };

export default function ManagerReportsPage() {
  return <ManagerFeaturePage feature="reports" />;
}
