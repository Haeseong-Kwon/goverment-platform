import { ManagerFeaturePage } from "@/features/startup-workspace/ManagerScreens";

export const metadata = { title: "사업비 계획 검토" };

export default function ManagerPlanReviewPage() {
  return <ManagerFeaturePage feature="plan-review" />;
}
