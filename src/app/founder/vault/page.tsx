import { FounderFeaturePage } from "@/features/startup-workspace/components";

export const metadata = { title: "서류 보관함" };

export default function FounderVaultPage() {
  return <FounderFeaturePage feature="vault" />;
}
