import { FounderFeaturePage } from "@/features/startup-workspace/components";

export const metadata = { title: "AI 진단" };

export default function FounderDiagnosticsPage() {
  return <FounderFeaturePage feature="diagnostics" />;
}
