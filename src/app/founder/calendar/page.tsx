import { FounderFeaturePage } from "@/features/startup-workspace/components";

export const metadata = { title: "마감 캘린더" };

export default function FounderCalendarPage() {
  return <FounderFeaturePage feature="calendar" />;
}
