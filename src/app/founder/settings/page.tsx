import { FounderFeaturePage } from "@/features/startup-workspace/components";

export const metadata = { title: "팀 설정" };

export default function FounderSettingsPage() {
  return <FounderFeaturePage feature="settings" />;
}
