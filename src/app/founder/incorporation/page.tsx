import { FounderFeaturePage } from "@/features/startup-workspace/components";

export const metadata = { title: "법인 설립" };

export default function FounderIncorporationPage() {
  return <FounderFeaturePage feature="incorporation" />;
}
