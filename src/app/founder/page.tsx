import { FounderCore } from "@/features/startup-workspace/components";

export const metadata = { title: "창업자 준비 워크스페이스" };

export default function FounderWorkspacePage() {
  return <FounderCore />;
}
