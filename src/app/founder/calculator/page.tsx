import { FounderFeaturePage } from "@/features/startup-workspace/components";

export const metadata = { title: "4대보험 계산기" };

export default function FounderCalculatorPage() {
  return <FounderFeaturePage feature="calculator" />;
}
