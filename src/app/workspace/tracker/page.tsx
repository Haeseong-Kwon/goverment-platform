import { FounderFeaturePage } from "@/features/startup-workspace/components";

export const metadata = { title: "상태 트래커" };

export default function WorkspaceTrackerPage() {
  return <FounderFeaturePage feature="tracker" founder />;
}
