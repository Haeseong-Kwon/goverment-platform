import { FounderFeaturePage } from "@/features/startup-workspace/components";

export const metadata = { title: "정산 사전검증" };

export default function WorkspacePrecheckPage() {
  return <FounderFeaturePage feature="precheck" founder />;
}
