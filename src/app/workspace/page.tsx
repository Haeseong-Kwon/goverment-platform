import { FounderCore } from "@/features/startup-workspace/components";

export const metadata = { title: "협약 수행 홈" };

export default function FounderAgreementWorkspacePage() {
  return <FounderCore founder />;
}
